import {
  StyleSheet,
  View,
} from 'react-native';
import React from 'react';

const MainScreen = () => {
  return <View style={{flex: 1, padding: 8}}></View>;
};

export default MainScreen;

const styles = StyleSheet.create({});
